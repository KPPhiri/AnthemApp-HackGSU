<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.philipphiri.hackgsuanthemapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoadingScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Emotions" />
        <activity
            android:name=".emoji_response"
            android:label="Feeling…" />
        <activity
            android:name=".elaborate_activity"
            android:label="Feeling..." />
        <activity
            android:name=".Vitals_Activity"
            android:label="My Vitals" />
        <activity
            android:name=".Feed_Activity"
            android:label="Feed" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".aboutPage"></activity>
    </application>

</manifest>